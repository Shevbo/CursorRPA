# Контекст установки Cursor Agent / RPA

Прикладывайте этот файл в чат с Ассистентом при продолжении настройки на **dev-VM** или **VDS**, вместе с выводом релевантных команд и текстом ошибок.

---

## Метаданные

| Поле | Значение (заполняйте вы) |
|------|---------------------------|
| Дата обновления | YYYY-MM-DD |
| Роль машины | dev-VM (VHDX) / VDS |
| Hostname | |
| IP / DNS | Пример VDS: `172.24.71.157` (доступ из вашей сети/VPN) |
| Пользователь ОС для RPA | |
| Дистрибутив | Напр. Ubuntu 24.04.2 LTS |

---

## Паритет dev ↔ VDS

| Проверка | Эталон (dev-VM) | Текущая машина | Совпало? |
|----------|------------------|----------------|----------|
| `lsb_release -a` | | | да/нет |
| `agent --version` | | | да/нет |
| Базовый путь workspaces | Напр. `/srv/workspaces` | | да/нет |
| Пользователь/группа владельца каталогов | | | да/нет |
| systemd юниты RPA (имена) | | | да/нет |

Расхождения (кратко):

---

## Cursor Agent CLI

| Поле | Значение |
|------|----------|
| Версия (`agent --version`) | |
| `agent status` (без секретов; обезличить) | |
| Где задаётся `CURSOR_API_KEY` | Напр. `~/.config/cursor-rpa/env.sh` (не коммитить) |

---

## Workspaces (проекты)

Список путей и назначение:

1. 
2. 

Правила имён для RPA (идентификатор → путь):

---

## Чаты (если уже есть)

| Проект (путь) | CHAT_ID | Примечание |
|---------------|---------|------------|
| | | |

---

## RPA

| Поле | Значение |
|------|----------|
| Коды команд и смысл | Напр. QUERY / APPLY / NEW_CHAT |
| Транспорт вызова | HTTP / очередь / systemd / другое |
| Порт или socket | |

---

## Последняя открытая задача

Опишите одним абзацем, что делаете сейчас:

---

## Чеклист «уже сделано»

- [ ] apt базовые пакеты
- [ ] установка `curl https://cursor.com/install | bash`
- [ ] `CURSOR_API_KEY` настроен
- [ ] `agent status` успешно
- [ ] Тест `agent -p --trust` в workspace
- [ ] `create-chat` / `ls` / `--resume` проверены
- [ ] Каталоги `/srv/workspaces/...`
- [ ] (VDS) паритет с dev сверен

---

## Лог последней ошибки

Вставьте **полный** текст (можно обрезать ключи):

```
(сюда)
```
